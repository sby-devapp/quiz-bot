# Core Telegram bot library (install latest available)
python-telegram-bot
python-telegram-bot[job-queue]

# Optional but useful for configuration and HTTP requests (install latest available)
python-dotenv
requests
